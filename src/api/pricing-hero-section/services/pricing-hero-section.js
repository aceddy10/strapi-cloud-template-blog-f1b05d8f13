'use strict';

/**
 * pricing-hero-section service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pricing-hero-section.pricing-hero-section');
