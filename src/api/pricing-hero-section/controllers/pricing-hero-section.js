'use strict';

/**
 * pricing-hero-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pricing-hero-section.pricing-hero-section');
