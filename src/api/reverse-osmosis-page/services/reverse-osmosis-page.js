'use strict';

/**
 * reverse-osmosis-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reverse-osmosis-page.reverse-osmosis-page');
