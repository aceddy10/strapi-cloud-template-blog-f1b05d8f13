'use strict';

/**
 * reverse-osmosis-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reverse-osmosis-page.reverse-osmosis-page');
