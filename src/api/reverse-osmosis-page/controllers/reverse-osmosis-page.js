'use strict';

/**
 * reverse-osmosis-page controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reverse-osmosis-page.reverse-osmosis-page');
