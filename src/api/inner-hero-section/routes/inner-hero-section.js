'use strict';

/**
 * inner-hero-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::inner-hero-section.inner-hero-section');
