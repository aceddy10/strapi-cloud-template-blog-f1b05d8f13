'use strict';

/**
 * reverse-osmosis-price service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::reverse-osmosis-price.reverse-osmosis-price');
