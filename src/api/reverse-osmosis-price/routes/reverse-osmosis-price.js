'use strict';

/**
 * reverse-osmosis-price router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::reverse-osmosis-price.reverse-osmosis-price');
