'use strict';

/**
 * reverse-osmosis-price controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::reverse-osmosis-price.reverse-osmosis-price');
